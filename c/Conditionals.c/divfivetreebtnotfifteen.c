#include<stdio.h>
void main(){
    int a;
    printf("Put a number ");
    scanf("%d",&a);
    
if (a%5==0 && a%3==0 || (a%15!=0)){

    printf("The number is divisible ");}

    else {
   printf ("The number is not divisble");}

    getch ();

}