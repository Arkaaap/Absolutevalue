#include <stdio.h>
int main () {
    int a;
    printf("Give a number");
    scanf("%d",&a);
    if (a>99 && a<1000)
    printf("The number is three digit ");
     else
   printf ("The number is not ");
    return 0 ;
}